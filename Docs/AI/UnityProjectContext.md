# Unity project context

Analyzed 2026-09-06. Root: `C:/Users/User/Documents/UNITY/talkback_test`.
Git has no commits; all original project files are untracked. No existing project instructions or gameplay scripts were found.

- Confirmed Unity 6000.4.1f1 (`ProjectSettings/ProjectVersion.txt`).
- URP 17.4.0 / 2D template, existing camera and Global Light 2D in `Assets/Scenes/SampleScene.unity`.
- Input System 1.19.0, activeInputHandler = 1 (new system only); uGUI 2.0.0.
- Unity Test Framework 1.6.0 installed. No original tests, asmdefs, networking code, or UI.
- Only enabled build scene: `Assets/Scenes/SampleScene.unity`.
- Unity process available locally; no callable Unity MCP. Original Editor log contains a disposed CancellationTokenSource message; no gameplay compile baseline existed.
- MVP addition uses namespace `NamnyeoChilse`, private serialized fields, plain C# round/name rules, MonoBehaviour lifecycle, and generated uGUI under an explicit scene component.
- Android TalkBack now uses built-in UnityEngine.Accessibility through AccessibleButtonGroup/AccessibleButtonBinding. Child uGUI Text labels and Button.onClick are reused; Bootstrap adds one group to GameCanvas, GameUI limits result modal navigation. Android API26+ publishes the hierarchy; Editor skips native activation. CanvasGroup raycast suppression avoids Unity's synthesized tap double dispatch. No app TTS, external native plugin, package or platform setting changes.
- TalkBack extension validation: 40 PlayMode tests passed (101.598 seconds); Android IL2CPP/ARM64 Development APK succeeded with zero build errors. APK minSdk25/targetSdk36; Unity raised clone minSdk23 to25 on Android import, original untouched. No connected adb device, so actual TalkBack speech/double-tap remain unverified. See Docs/Android-TalkBack.md.
- Name/audio data loads via ResourceNameLoader and Resources.LoadAll<AudioClip>("Names"). Filename suffix 수/희 selects Male/Female; invalid/duplicate names are warned and skipped. GameBootstrap has no manual Names field. NameManager preserves 50/50 gender weighting and silent defaults if empty.
- NameAudioPlayer manages a required 2D AudioSource on the game object. GameManager owns one playback coroutine and CanAcceptInput; GameUI mirrors that gate. Input opens after source.isPlaying becomes false, silent names yield before input, and expiry/restart/disable cancel playback. GameRound rules remain unchanged. Playback PlayMode suite: 12 passed (67.580 seconds), including actual engine completion with pitch 0.5, cancellation, and a real 60-second round; no device/listening verification.
- Resources/Names contains 20 WAVs converted from user-provided M4As. Original files and metas preserved at Assets/AudioSource/Names/{boys,girls}. BGM/SFX unchanged. M4A was DefaultImporter rather than AudioClip in this environment.
- Combo scoring: GameRound increments Combo before applying ScoringSettings, resets only Combo on wrong answers, and clears Score/Combo on Start. Inspector GameBootstrap.Scoring supplies base score and minimum-combo/multiplier tiers (100; 0:x1, 5:x2, 10:x3, 15:x4). UI shows combo, multiplier, and actual award. No audio speed or sequencing change.
- Sequence extension: GameBootstrap.SequenceSettings configures 0:1, 5:2, 10:3, 15:4 names per question. GameRound stores read-only Sequence/ExpectedAnswers and InputIndex; Progress means a partial correct answer, with scoring only on full success. NameManager.NextSequence draws without replacement and caps to distinct catalog size. GameManager plays every clip before opening ordered input; expiry clears sequence/history and cancels pending playback. UI shows playback/input position. Audio speed/spacing unchanged.
- Audio interval extension: same SequenceTier now stores audioInterval (0:1.0s, 5:0.7s, 10:0.4s, 15:0.15s). GameRound snapshots it per question. GameManager waits in real time only after a completed clip when another name remains, within the existing cancellable playback coroutine. Pitch unchanged; no leading/trailing/inter-question delay. Empty/invalid tier fallback is one name/1 second.

Sources: Packages/manifest.json, ProjectSettings/{ProjectVersion.txt,EditorBuildSettings.asset,ProjectSettings.asset,GraphicsSettings.asset}, Assets/Scenes/SampleScene.unity, Assets/Settings, Assets/InputSystem_Actions.inputactions.

Main menu extension: GameBootstrap creates MainScreen/GameplayScreen/RankingScreen/NicknameScreen under the same SafeArea. MainMenuNavigation keeps one screen active, starts the initially disabled GameManager only on Game Start, and returns from results with GameManager disabled. GameUI now lives on GameplayScreen and finds the accessibility group in its parent. One shared AccessibleButtonGroup automatically labels all child buttons; transitions clear EventSystem selection and refresh active accessibility scope. Ranking remains a placeholder. No gameplay rule or scene/settings edits. See Docs/Main-Menu.md.

UGS extension: official Authentication3.7.4/Core1.18.0, existing cloudProjectId retained. PlayerAccountHost starts separate PlayerAccountService backed by UgsPlayerAccountBackend; cached anonymous sign-in precedes GetPlayerNameAsync(false)/UpdatePlayerNameAsync. Server-returned full name is authoritative, errors preserve identity/name, no token clearing. NicknameUI wires drafts/confirm/cancel and announcements; AccessibleTextInput opens native Android AlertDialog/EditText using TextInputDialog.java, while Editor uses uGUI. AccessibleButtonGroup now supports the input entry alongside buttons. 49 offline PlayMode tests passed; separate codex-validation SDK profile verified real server save and same-ID/name restoration across processes. See Docs/UGS-Authentication-Nickname.md.

Audio speed/feedback update supersedes interval defaults above: all default intervals and SampleScene tiers are now zero; playbackSpeed 1/1.2/1.4/1.6 is snapshotted per sequence. AnswerAudioPlayer automatically loads Resources/Feedback/{sfx_correct,sfx_line_wrong}.wav, converted from preserved project M4As, and owns a separate child source at pitch1. GameManager waits for feedback for every accepted input (partial success opens next input; terminal result begins next question), cancelling both sources on expiry/restart/disable. See Docs/Audio-Speed-Feedback.md.

Whole-sequence judgement update: GameRound.Submit now records every input and returns Progress without comparison until the required count is reached; then compares the entire array exactly once. GameManager uses a silent one-frame duplicate-event guard for Progress and captures PlaybackSpeed before final Submit changes the tier. AnswerAudioPlayer uses that captured speed for both correct/wrong feedback; feedback completion starts the next problem without the former initial frame delay. No new Inspector fields or scene edits. Updated runtime files: GameRound, GameManager, AnswerAudioPlayer. Updated tests: SequenceTests, ComboScoringTests, AudioIntervalTests, AnswerPlaybackTests.

Feedback overlap supersedes the serial feedback flow: GameManager immediately starts the next name and calls AnswerAudioPlayer.Play independently, never awaiting feedback. The feedback owns its cancellable coroutine; expiry/restart/disable stop both sources, and new feedback replaces any old feedback. Whole-array judgement and captured old-question pitch remain unchanged.

Leaderboards upload: package 2.3.3 added with existing Authentication3.7.4/Core1.18.0 retained. GameManager exposes once-per-normal-completion Completed(gameNumber,score), disarmed on disable/abandonment. Bootstrap wires LeaderboardScoreHost to the existing account service; LeaderboardScoreService authenticates through it, consumes duplicate game numbers before await, catches errors, and calls UgsLeaderboardScoreBackend.AddPlayerScoreAsync. ID is centralized in LeaderboardSettings: namnyeo_chilse_high_score. Server must use highest-to-lowest Keep Best, no resets/buckets. No ranking UI. Dashboard resource provisioning and actual server verification remain user setup; tests use injected fake servers. See Docs/Leaderboard-Upload.md.

Leaderboards final validation: 58 PlayMode tests passed (88.242 seconds), Validation/leaderboard-final-results.xml; official registry package resolved, source/package hashes match validation project. Actual server Keep Best verification pending Dashboard creation. Last provided APK predates Leaderboards integration.

Online ranking UI: RankingUI on RankingScreen fetches on enable/refresh using RankingService (single in-flight task, authentication gate) and UgsRankingBackend (GetScoresAsync offset0 limit10; GetPlayerScoreAsync; EntryNotFound only is missing personal score). Reuses LeaderboardSettings ID. Top/my results have independent failures. Close/dispose invalidates generation so old responses cannot mutate hidden views. Ten visible rows use AccessibleReadOnlyText/StaticText with full rank,name,score labels via existing group/binding; visually long names truncate. Status and personal record are also accessible. Game/score upload untouched, no packages added. 60 PlayMode tests passed (88.232s); actual UGS data and Android TalkBack device testing still require configured board/device. See Docs/Online-Ranking.md.

Startup loading gate: Bootstrap deactivates MainScreen immediately on creation; LoadingScreen alone is initially visible/accessibility-modal. StartupLoadingUI owns the initial existing PlayerAccountService.InitializeAsync call (PlayerAccountHost no longer starts it separately), waits for both sign-in and name read, handles errors/retry, and only then calls MainMenuNavigation.CompleteStartup. Navigation rejects ShowMain until ready. Successful transition disables LoadingScreen, activates main, updates accessibility frames, selects StartGameButton and sends Android SendScreenChanged targeting its node. Repeated loading announcements are deduplicated; nickname announcements remain suppressed while its main display is inactive. No credentials reset or new auth backend. See Docs/Startup-Loading.md.

2026-09-07 UGS ranking investigation: real authenticated production queries returned LeaderboardsException 27005 (Leaderboard config could not be found) for namnyeo_chilse_high_score on cloud project 3aebe42d-021c-47a4-b77a-7b3e2b1f3aac. Added explicit production initialization, redacted Development UGS diagnostics and distinct missing-configuration UI. 64/64 PlayMode passed; diagnostic Android APK built with errors=0. See Docs/Leaderboard-Diagnostics.md. Dashboard creation remains required; physical device/upload success not verified.
